[project]
name = "federated-credentials-automation"
version = "0.1.0"
description = "Automate Azure AD federated credentials for GitHub Actions across multiple repositories"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["jupyter"]
